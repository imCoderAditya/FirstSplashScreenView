name: firstsplashscreenview
description: The flagship Flutter splash screen package with 100+ templates, Liquid Glass (iOS 26), GPU particle engine, animations, and 100% backward compatibility.
version: 1.0.1
homepage: https://github.com/imCoderAditya/FirstSplashScreenView.git
repository: https://github.com/imCoderAditya/FirstSplashScreenView.git
issue_tracker: https://github.com/imCoderAditya/FirstSplashScreenView/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
